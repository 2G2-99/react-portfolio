import { Container, Hero } from '../components/export';

const Home = () => {
	return (
		<>
			<Container className="container-fluid">
				<Hero />
			</Container>
		</>
	);
};

export default Home;
