import { Container, Hero } from '../components/export';

const Home = () => {
	return (
		<>
			<Container className={Container}>
				<Hero />
			</Container>
		</>
	);
};

export default Home;
